define([
    'jquery',
    'lib/underscore', 
    'lib/backbone', 
    'config', 
], function($, _, Backbone, config) {
    var pageInfo = Backbone.Model.extend({
    });
    
    return pageInfo;
});
